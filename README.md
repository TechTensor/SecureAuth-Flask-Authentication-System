# SecureAuth-Flask-Authentication-System
A secure Flask-based authentication system featuring user registration, Argon2 password hashing, SQLite database, session management, and optional Time-based One-Time Password (TOTP) two-factor authentication.
